# T3 Cost

Tiny browser extension for `t3.chat` that shows your usage period percentage while you chat.

## Local development

```bash
pnpm dev
```

Load the extension from `build/chrome-mv3-dev` in Chrome.

## Build

```bash
pnpm build
```
